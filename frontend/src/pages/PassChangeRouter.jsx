import { Outlet } from "react-router-dom";
const PassChangeRouterPage = () => {
  return (
    <main className="changePassword">
      <Outlet />
    </main>
  );
};

export default PassChangeRouterPage;
