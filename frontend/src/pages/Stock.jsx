// import { useAuthorization } from "../../hooks/useAuthorization.js";

const STOCK = import.meta.env.VITE_STOCK;
const StockPage = () => {
  // useAuthorization(STOCK);

  return <h1>stock management</h1>;
};

export default StockPage;
